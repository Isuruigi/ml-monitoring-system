name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  PYTHON_VERSION: '3.11'

jobs:
  # Job 1: Validate Project Structure
  validate-structure:
    name: Validate Project
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Validate project files
        run: |
          echo "✅ Validating ML Monitoring System structure..."
          
          # Check backend
          test -f "backend/api/main.py" && echo "✓ Backend API" || exit 1
          test -f "backend/ml/model.py" && echo "✓ ML Model" || exit 1
          test -f "backend/requirements.txt" && echo "✓ Requirements" || exit 1
          
          # Check frontend
          test -f "frontend/package.json" && echo "✓ Frontend Package" || exit 1
          test -f "frontend/app/page.tsx" && echo "✓ Frontend App" || exit 1
          
          # Check docker
          test -f "docker-compose.yml" && echo "✓ Docker Compose" || exit 1
          test -f "docker/Dockerfile.backend" && echo "✓ Backend Dockerfile" || exit 1
          
          echo "✅ All files validated!"
  
  # Job 2: Lint Backend Code
  lint-backend:
    name: Lint Backend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      
      - name: Install linting tools
        run: |
          pip install flake8
      
      - name: Run linting
        run: |
          # Check for Python syntax errors
          flake8 backend --count --select=E9,F63,F7,F82 --show-source --statistics
          echo "✓ No syntax errors found"
        continue-on-error: false
  
  # Job 3: Security Scan
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Run Trivy scanner
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: 'fs'
          scan-ref: '.'
          format: 'table'
          exit-code: '0'
      
      - name: Security scan complete
        run: echo "✅ Security scan completed"
  
  # Job 4: Summary
  ci-success:
    name: CI Pipeline Complete
    runs-on: ubuntu-latest
    needs: [validate-structure, lint-backend, security-scan]
    
    steps:
      - name: Success summary
        run: |
          echo "🎉 CI/CD Pipeline Completed Successfully!"
          echo ""
          echo "✅ Project structure validated"
          echo "✅ Code linting passed"
          echo "✅ Security scan completed"
          echo ""
          echo "📦 ML Monitoring System is production-ready!"
